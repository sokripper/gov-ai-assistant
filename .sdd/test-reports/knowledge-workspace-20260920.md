# 知识库原型验证 · 2026-09-20

## 结果

新增 37 项完整脚本集成测试通过；既有 213 项测试通过。仅代表 DOM、状态、样式契约及文案检查，不代表浏览器像素验收或真实服务端联调。

## 新版范围

- 营运总览：三个指标组、14天用量、年度提交、当前账户可查看文件的搜索/来源筛选/处理状态筛选/空态/直达与重置；不显示请求日志及 API 技术字段。
- 个人、共享和部门知识库：两栏结构、目录展开、名称/摘要筛选、摘要与正文、新建表单、空值和同名校验。
- 部门选择、缺库提示、禁止重复建库、按部门展示隔离。
- 文件选择、移除、取消、TXT实际读取、PDF待处理、同名防覆盖、异步读取期间取消及 Escape、原文件下载。
- 问答自动范围、多轮、当前浏览器会话历史、新建、停止、页面切换中断、引用内容和复制、无资料提示。
- 递归删除与后续问答一致性；旧路由、首页与最近会话入口兼容；不存在内部平台的真实数据、地址或密钥。

## 回归

prototype-interactions 45；wechat-layouts 69；wechat-studio 25；wechat-real-case 17；wechat-photo-overlay 11；wechat-image-sizing 9；notice-format 15；product-wording 10；wechat-gallery 12。

运行时：Node.js；jsdom 使用既有临时测试依赖。新增测试入口：`scripts/test-knowledge-workspace.cjs`。

## 限制

- 参考平台仅通过浏览器操作观察。未修改其目录、文件或部门配置；发送一条普通功能核对问题后停止等待，未取得完整答案。
- 不能据此推断后端能力、真实账号权限、数据安全隔离或模型回答质量。
- 本地页面先前受 Browser Use URL 策略限制，本轮没有换入口规避，也未完成像素级浏览器验收。
- 本轮仅修改本地原型，未提交或推送公开仓库；未触碰原有未跟踪素材。
